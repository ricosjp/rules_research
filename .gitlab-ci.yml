image: mirisbowring/texlive_ctan_full:2019

stages:
  - deploy

build:
  stage: deploy
  script:
    - make pdf -j
    - mkdir -p ./public/
    - find . -name '*.pdf' | xargs -I% mv % ./public/
  artifacts:
    paths:
    - public
